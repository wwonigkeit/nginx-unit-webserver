package unit

import "fmt"

//ExternalBuild executes the build for Go or NodeJS applications
//It takes the external unmarshalled JSON object and the client
//object to relay the build progress
func ExternalBuild(extJSONObj *External) {

	fmt.Println("Build the go platform")

}
